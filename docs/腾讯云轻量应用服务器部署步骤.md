# 腾讯云轻量应用服务器部署步骤

服务器公网 IP：`119.45.196.92`

当前项目部署结构：

```text
浏览器 -> Nginx:80
静态页面 -> /www/wwwroot/mes/frontend/dist
/api/* -> Nginx 反向代理到 127.0.0.1:8080
后端入口 -> com.example.messystem.MesBackendApplication
进程守护 -> Supervisor
```

公网只需要访问：

```text
http://119.45.196.92/
```

后端 `8080` 端口只给服务器本机 Nginx 访问，不建议对公网开放。

## 1. 安装运行环境

Ubuntu/Debian：

```bash
sudo apt update
sudo apt install -y openjdk-21-jdk maven git nginx supervisor
java -version
mvn -version
```

必须确认 Java 是 JDK 21。

## 2. 拉取项目代码

```bash
sudo mkdir -p /www/wwwroot /www/wwwlogs
cd /www/wwwroot
sudo git clone <你的远程仓库地址> mes
sudo chown -R root:root /www/wwwroot/mes
```

后续统一使用：

```text
/www/wwwroot/mes
```

## 3. 配置数据库环境变量

```bash
cd /www/wwwroot/mes
sudo cp deploy/env.production.example .env
sudo nano .env
```

填写真实数据库：

```env
MES_DB_HOST=你的 PostgreSQL 地址
MES_DB_PORT=5432
MES_DB_NAME=MESSystem
MES_DB_USER=MESSystem
MES_DB_PASSWORD=你的数据库密码
MES_HOST=127.0.0.1
MES_PORT=8080
MES_OPEN_BROWSER=false
MES_PUBLIC_URL=http://119.45.196.92/
```

`.env` 不要提交到 Git。

## 4. 测试后端入口

```bash
cd /www/wwwroot/mes
chmod +x scripts/run-backend.sh
MES_HOST=127.0.0.1 MES_PORT=8080 MES_OPEN_BROWSER=false ./scripts/run-backend.sh
```

看到类似输出表示后端启动成功：

```text
MES backend started: http://127.0.0.1:8080/
Listening on 127.0.0.1:8080
```

另开一个 SSH 窗口测试首页：

```bash
curl -I http://127.0.0.1:8080/
```

测试完成后按 `Ctrl+C` 停止。

## 5. 配置 Supervisor

```bash
sudo cp /www/wwwroot/mes/deploy/supervisor/mes-backend.conf /etc/supervisor/conf.d/mes-backend.conf
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl status
```

常用命令：

```bash
sudo supervisorctl restart mes-backend
sudo supervisorctl stop mes-backend
sudo supervisorctl start mes-backend
```

日志：

```bash
sudo tail -n 100 /www/wwwlogs/mes-backend-error.log
sudo tail -n 100 /www/wwwlogs/mes-backend-access.log
```

## 6. 配置 Nginx

```bash
sudo cp /www/wwwroot/mes/deploy/nginx/mes.conf /etc/nginx/sites-available/mes.conf
sudo ln -sfn /etc/nginx/sites-available/mes.conf /etc/nginx/sites-enabled/mes.conf
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl reload nginx
```

## 7. 验证部署

服务器内部验证：

```bash
curl -I http://127.0.0.1:8080/
curl -I http://127.0.0.1/
sudo supervisorctl status mes-backend
```

`/api/db/ping` 已受登录和 `system.health.read` 权限保护。请在浏览器登录管理员账号，再从系统维护页面检查数据库状态。

浏览器访问：

```text
http://119.45.196.92/
```

## 8. 后续更新部署

```bash
cd /www/wwwroot/mes
sudo git pull
sudo chmod +x scripts/run-backend.sh
sudo supervisorctl restart mes-backend
sudo nginx -t
sudo systemctl reload nginx
```

如果只改了前端静态页面，通常重载 Nginx 即可；如果改了 Java 后端代码，要重启 `mes-backend`。

使用压缩包更新时，不要覆盖服务器现有 `.env` 和 `storage`。将本机生成的发布包与 `deploy/install-release.sh` 上传到服务器 `/tmp`，然后执行：

```bash
cd /tmp
sha256sum -c SHA256SUMS.txt
sudo bash install-release.sh /tmp/mes-web-v1-20260717.tar.gz
```

脚本会先在临时目录解压和编译，继承现有 `.env` 与 `storage`，再备份旧目录并切换。Supervisor、Nginx、HTTP 或数据库只读健康检查失败时，会自动恢复旧目录和旧配置。
