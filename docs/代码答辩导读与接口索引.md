# 代码答辩导读与接口索引

> 本文件由 `node scripts/generate-defense-code-index.mjs` 从 JAX-RS 源码生成。接口以源码注解为准；SQL 文件按任务要求不纳入注释与索引。

## 一、系统从浏览器到数据库的调用链

```text
Vue 页面/ModuleWorkspace
  -> frontend/src/api/http.js（/api + Bearer Token + ApiResponse 解包）
  -> Nginx 或内嵌 Tomcat
  -> AuthFilter（身份） -> AuthorizationPolicy（接口权限） -> DataScopeService（数据范围）
  -> *Resource（HTTP 参数与响应）
  -> *Service（校验、状态机、用例编排）
  -> *Dao（PreparedStatement、事务、行映射）
  -> Db/DbConfig -> PostgreSQL 或云 RDS
```

答辩要点：前端隐藏菜单/按钮只改善体验；真正的安全边界在后端。Controller 不写 SQL，Service 不依赖 HTTP/JDBC，DAO 不依赖 JAX-RS，这些约束由 `ArchitectureLayerTest` 固化。

## 二、技术栈与外部依赖

| 层次 | 技术/依赖 | 在代码中的入口 | 作用 |
|---|---|---|---|
| 前端 | Vue 3、Composition API | `frontend/src/main.js`、`.vue` | 组件、响应式状态与模板 |
| 前端状态/路由 | Pinia、Vue Router | `stores/session.js`、`router.js` | 会话持久化、登录守卫、History 路由 |
| 前端构建 | Vite | `frontend/vite.config.js` | 5173 开发服务器、`/api` 代理、生产 `dist` |
| HTTP | Jakarta REST 4、Jersey 3 | `MesApplication`、`*Resource` | JAX-RS 路由、JSON 接口、过滤器 |
| Web 容器 | Tomcat 10.1 / Servlet 6 | `MesBackendApplication`、`web.xml` | 内嵌启动、WAR 兼容、静态文件 |
| JSON | Jackson 2.17 | `JacksonConfig` | entity/record 与 JSON、Java 时间转换 |
| 数据库 | JDBC、PostgreSQL 42.7 | `DbConfig`、`Db`、`*Dao` | 云数据库连接、预编译 SQL、事务 |
| 安全 | PBKDF2-HMAC-SHA256、随机 Token 摘要 | `PasswordHasher`、`TokenHasher`、`AuthFilter` | 密码加盐哈希、会话、RBAC 默认拒绝 |
| 文件 | ZXing、Java2D、PDFBox | `TraceFileService` | 二维码、标签 PNG、产品 PDF、SHA-256 |
| AI | 阿里云百炼 OpenAI Compatible API | `AiPlanningClient`、`AiPlanningConfig` | PMC 排产建议；只建议、不自动落库 |
| 测试 | JUnit 5、Node assert、Playwright、Newman | `backend/src/test`、`frontend/scripts`、`postman` | 单元、架构、UI、接口和报告 |
| 部署 | Nginx、Supervisor、Bash | `deploy/`、`scripts/run-backend.sh` | 静态资源、反代、守护、回滚与探针 |

## 三、模块与代码定位

| 模块 | 前端入口 | 后端目录 | 核心说明 |
|---|---|---|---|
| 认证与个人资料 | `/login`、`/profile`、`stores/session.js` | `auth/` | 登录、令牌、会话、当前用户、资料更新 |
| 首页与驾驶舱 | `/`、`/executive` | `dashboard/` | 岗位首页、经营指标、趋势、告警、反馈、产品追溯 |
| 计划与工单 | `/module/planning` | `planning/` | 客户订单、任务、齐套、缺料、返工计划、工单派发/接收、AI 建议 |
| 生产执行 | `/module/production` | `production/` | 报工新增/修改/审核/驳回与计件工资 |
| 仓储物流 | `/module/warehouse` | `warehouse/` | 主数据、库存、外采、领料审批/接收、拣货、机器人配送 |
| 质量管理 | `/module/quality` | `quality/` | 质检创建/分配/录项/提交/判定、返工、质量追溯 |
| 设备维护 | `/module/equipment` | `equipment/` | 设备台账、报修审核、维修派工/完工/验收、保养计划 |
| 工艺与主数据 | `/module/process` | `master/` | 产品、BOM、工艺路线、生产线、同步日志、用户 |
| 产品追溯 | `/module/trace`、`/trace-public` | `trace/` | 一胎一码、文件预览/打印、随机 token 公开追溯 |
| 用户与权限 | `/module/access` | `access/`、`security/` | 角色、权限、申请审核、数据范围、会话/账号维护、审计 |

## 四、启动、配置与部署代码

| 场景 | 文件/命令 | 说明 |
|---|---|---|
| 本地前端 | `cd frontend && npm run dev` | Vite 5173，将 `/api` 代理到 8080 |
| 本地后端 | `mvn -pl backend compile exec:java` | 启动 `MesBackendApplication`，默认 127.0.0.1:8080 |
| 配置 | `.env.example`、`DbConfig` | 系统属性 -> 环境变量 -> `.env` -> 默认值；真实 `.env` 不提交 |
| 生产前端/API | `deploy/nginx/mes.conf` | `dist` 静态文件、History 回退、`/api` 反代 |
| 进程守护 | `deploy/supervisor/mes-backend.conf` | 自动启动/重启、进程组停止、日志与 JDK 21 |
| 发布 | `deploy/install-release.sh` | 暂存编译、配置/存储继承、原子切换、探针、失败回滚 |
| Postman | `postman/generate-collection.mjs`、`run-collection.mjs` | 扫描 Resource，Newman 执行并生成脱敏答辩报告 |
| 源码注释 | `scripts/add-defense-comments.ps1` | 幂等补充 Java 分层、接口、方法和字段注释 |

## 五、严格 JSON 与生成/第三方文件说明

`package.json`、`package-lock.json`、Postman collection/environment、微信项目配置和测试 JSON 报告采用严格 JSON，语法不允许 `//` 或 `/* */`。为保证 npm、Postman、微信工具可正常解析，这些文件不写非法行内注释；其用途在本索引、相邻生成脚本及 README 中说明。`node_modules`、`target`、`dist`、运行日志、报告 HTML/XML、图片、Office 文档和 Maven Wrapper 属于第三方或生成产物，不按自有业务源码改写。

## 六、全量 HTTP 接口（173 个）

### 订单、任务、齐套、缺料、返工计划与工单

#### AiPlanningAdviceResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| POST | `/api/ai/planning/advice` | `advice()` | 执行 advice 对应的业务步骤 |

源码：`backend/src/main/java/com/example/messystem/planning/controller/AiPlanningAdviceResource.java`

#### CustomerOrderResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/orders` | `list()` | 查询列表 |
| POST | `/api/orders` | `create()` | 创建业务记录 |
| GET | `/api/orders/{id}` | `get()` | 查询单条记录或详情 |

源码：`backend/src/main/java/com/example/messystem/planning/controller/CustomerOrderResource.java`

#### ProductionTaskResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/production-tasks` | `list()` | 查询列表 |
| POST | `/api/production-tasks` | `create()` | 创建业务记录 |
| POST | `/api/production-tasks/{id}/kitting` | `analyzeKitting()` | 执行 analyzeKitting 对应的业务步骤 |
| POST | `/api/production-tasks/{id}/shortage-alerts` | `publishShortageAlerts()` | 执行 publishShortageAlerts 对应的业务步骤 |
| GET | `/api/production-tasks/kitting-analyses` | `listAnalyses()` | 查询列表 |
| GET | `/api/production-tasks/shortage-alerts` | `listAlerts()` | 查询列表 |

源码：`backend/src/main/java/com/example/messystem/planning/controller/ProductionTaskResource.java`

#### ReworkPlanningResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/planning/reworks` | `list()` | 查询列表 |
| POST | `/api/planning/reworks/{id}/tasks` | `plan()` | 执行 plan 对应的业务步骤 |

源码：`backend/src/main/java/com/example/messystem/planning/controller/ReworkPlanningResource.java`

#### ShortageAlertResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/shortage-alerts` | `list()` | 查询列表 |
| POST | `/api/shortage-alerts/{id}/accept` | `accept()` | 受理业务事项 |

源码：`backend/src/main/java/com/example/messystem/planning/controller/ShortageAlertResource.java`

#### WorkOrderResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/work-orders` | `list()` | 查询列表 |
| POST | `/api/work-orders` | `create()` | 创建业务记录 |
| GET | `/api/work-orders/{id}` | `get()` | 查询单条记录或详情 |
| POST | `/api/work-orders/{id}/dispatch` | `dispatch()` | 派发业务任务 |
| GET | `/api/work-orders/{id}/logs` | `logs()` | 执行 logs 对应的业务步骤 |
| POST | `/api/work-orders/{id}/receive` | `receive()` | 接收已派发任务 |
| GET | `/api/work-orders/logs` | `logs()` | 执行 logs 对应的业务步骤 |
| GET | `/api/work-orders/operators` | `operators()` | 执行 operators 对应的业务步骤 |

源码：`backend/src/main/java/com/example/messystem/planning/controller/WorkOrderResource.java`

### 健康检查与公共响应

#### DbPingResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/db/ping` | `ping()` | 执行数据库健康检查，返回连接信息和关键表可用状态，供部署探针与系统管理员诊断 |

源码：`backend/src/main/java/com/example/messystem/common/controller/DbPingResource.java`

### 角色首页、经营驾驶舱与管理反馈

#### DashboardResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/dashboard/executive` | `executive()` | 聚合经营指标、生产趋势、产线、告警、部门报告和审计发现，供总经理驾驶舱展示 |
| GET | `/api/dashboard/my-summary` | `mySummary()` | 按当前登录用户的岗位、权限和数据范围返回角色首页指标、待办与快捷入口 |

源码：`backend/src/main/java/com/example/messystem/dashboard/controller/DashboardResource.java`

#### ManagementFeedbackResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/management-feedback` | `listFeedback()` | 查询列表 |
| POST | `/api/management-feedback` | `createFeedback()` | 创建业务记录 |
| GET | `/api/management-feedback/{id}` | `getFeedback()` | 查询单条记录或详情 |
| POST | `/api/management-feedback/{id}/close` | `closeFeedback()` | 关闭业务事项 |
| GET | `/api/management-feedback/create-options` | `createOptions()` | 创建业务记录 |

源码：`backend/src/main/java/com/example/messystem/dashboard/controller/ManagementFeedbackResource.java`

#### ProductTraceResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/product-traces` | `listTraces()` | 查询列表 |
| POST | `/api/product-traces` | `createTrace()` | 创建业务记录 |
| GET | `/api/product-traces/{id}` | `getTrace()` | 查询单条记录或详情 |
| GET | `/api/product-traces/work-orders/{workOrderId}` | `listByWorkOrder()` | 查询列表 |

源码：`backend/src/main/java/com/example/messystem/dashboard/controller/ProductTraceResource.java`

### 轮胎标签、文件与公开追溯

#### PublicTireTraceResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/public/tire-traces/{token}` | `get()` | 查询单条记录或详情 |
| GET | `/api/public/tire-traces/{token}/document` | `document()` | 执行 document 对应的业务步骤 |

源码：`backend/src/main/java/com/example/messystem/trace/controller/PublicTireTraceResource.java`

#### TireLabelResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/tire-labels` | `list()` | 查询列表 |
| GET | `/api/tire-labels/{id}/document` | `document()` | 执行 document 对应的业务步骤 |
| GET | `/api/tire-labels/{id}/label` | `label()` | 读取标签图片 |
| POST | `/api/tire-labels/{id}/print` | `recordPrint()` | 执行 recordPrint 对应的业务步骤 |
| GET | `/api/tire-labels/{id}/qrcode` | `qrCode()` | 读取二维码文件 |
| POST | `/api/tire-labels/generate` | `generate()` | 生成业务结果 |
| GET | `/api/tire-labels/generate-options` | `generateOptions()` | 生成业务结果 |

源码：`backend/src/main/java/com/example/messystem/trace/controller/TireLabelResource.java`

### 认证、会话与个人资料

#### AuthResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| POST | `/api/auth/login` | `login()` | 校验账号密码并创建会话 |
| POST | `/api/auth/logout` | `logout()` | 注销当前会话 |
| GET | `/api/auth/me` | `me()` | 执行 me 对应的业务步骤 |

源码：`backend/src/main/java/com/example/messystem/auth/controller/AuthResource.java`

#### ProfileResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/profile` | `get()` | 查询单条记录或详情 |
| PUT | `/api/profile` | `update()` | 更新业务记录 |

源码：`backend/src/main/java/com/example/messystem/auth/controller/ProfileResource.java`

### 设备、报修、维修与保养

#### EquipmentRepairReportResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/equipment-repair-reports` | `list()` | 查询列表 |
| POST | `/api/equipment-repair-reports` | `create()` | 创建业务记录 |
| POST | `/api/equipment-repair-reports/{id}/approve` | `approve()` | 审核通过业务事项 |
| POST | `/api/equipment-repair-reports/{id}/to-maintenance-order` | `toMaintenanceOrder()` | 执行 toMaintenanceOrder 对应的业务步骤 |

源码：`backend/src/main/java/com/example/messystem/equipment/controller/EquipmentRepairReportResource.java`

#### EquipmentResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/equipment` | `listEquipment()` | 查询列表 |
| POST | `/api/equipment` | `createEquipment()` | 创建业务记录 |
| GET | `/api/equipment/{id}` | `getEquipment()` | 查询单条记录或详情 |
| PUT | `/api/equipment/{id}/status` | `updateStatus()` | 更新业务记录 |
| GET | `/api/equipment/by-line/{lineId}` | `listByLine()` | 查询列表 |

源码：`backend/src/main/java/com/example/messystem/equipment/controller/EquipmentResource.java`

#### MaintenanceOrderResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/maintenance-orders` | `list()` | 查询列表 |
| POST | `/api/maintenance-orders/{id}/accept` | `accept()` | 受理业务事项 |
| POST | `/api/maintenance-orders/{id}/assign` | `assign()` | 分配执行人员或资源 |
| POST | `/api/maintenance-orders/{id}/finish` | `finish()` | 完成业务任务 |
| GET | `/api/maintenance-orders/maintainers` | `maintainers()` | 执行 maintainers 对应的业务步骤 |

源码：`backend/src/main/java/com/example/messystem/equipment/controller/MaintenanceOrderResource.java`

#### MaintenancePlanResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/maintenance-plans` | `list()` | 查询列表 |
| POST | `/api/maintenance-plans` | `create()` | 创建业务记录 |

源码：`backend/src/main/java/com/example/messystem/equipment/controller/MaintenancePlanResource.java`

### 生产报工与计件工资

#### PieceworkWageResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/piecework-wages` | `list()` | 查询列表 |
| GET | `/api/piecework-wages/{id}` | `get()` | 查询单条记录或详情 |
| GET | `/api/piecework-wages/by-report/{workReportId}` | `listByReport()` | 查询列表 |

源码：`backend/src/main/java/com/example/messystem/production/controller/PieceworkWageResource.java`

#### WorkReportResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/work-reports` | `list()` | 查询列表 |
| POST | `/api/work-reports` | `create()` | 创建业务记录 |
| DELETE | `/api/work-reports/{id}` | `delete()` | 删除业务记录 |
| GET | `/api/work-reports/{id}` | `get()` | 查询单条记录或详情 |
| PUT | `/api/work-reports/{id}` | `update()` | 更新业务记录 |
| POST | `/api/work-reports/{id}/approve` | `approve()` | 审核通过业务事项 |
| POST | `/api/work-reports/{id}/reject` | `reject()` | 驳回业务事项 |
| GET | `/api/work-reports/by-work-order/{workOrderId}` | `listByWorkOrder()` | 查询列表 |

源码：`backend/src/main/java/com/example/messystem/production/controller/WorkReportResource.java`

### 物料、仓库、库存、领料、拣货与机器人物流

#### InventoryResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/inventory` | `list()` | 查询列表 |
| POST | `/api/inventory` | `create()` | 创建业务记录 |
| DELETE | `/api/inventory/{id}` | `delete()` | 删除业务记录 |
| GET | `/api/inventory/{id}` | `get()` | 查询单条记录或详情 |
| PUT | `/api/inventory/{id}` | `update()` | 更新业务记录 |
| POST | `/api/inventory/external-purchase` | `externalPurchase()` | 模拟外部采购入库 |
| GET | `/api/inventory/material/{materialId}` | `listByMaterial()` | 查询列表 |
| GET | `/api/inventory/transactions` | `listTransactions()` | 查询列表 |
| POST | `/api/inventory/transactions` | `createTransaction()` | 创建业务记录 |
| GET | `/api/inventory/transactions/{id}` | `getTransaction()` | 查询单条记录或详情 |

源码：`backend/src/main/java/com/example/messystem/warehouse/controller/InventoryResource.java`

#### MaterialRequisitionResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/requisitions` | `list()` | 查询列表 |
| POST | `/api/requisitions` | `create()` | 创建业务记录 |
| GET | `/api/requisitions/{id}` | `get()` | 查询单条记录或详情 |
| POST | `/api/requisitions/{id}/approve` | `approve()` | 审核通过业务事项 |
| POST | `/api/requisitions/{id}/receive` | `receive()` | 接收已派发任务 |
| POST | `/api/requisitions/{id}/reject` | `reject()` | 驳回业务事项 |
| GET | `/api/requisitions/by-work-order/{workOrderId}` | `listByWorkOrder()` | 查询列表 |
| GET | `/api/requisitions/create-options` | `createOptions()` | 创建业务记录 |

源码：`backend/src/main/java/com/example/messystem/warehouse/controller/MaterialRequisitionResource.java`

#### MaterialResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/materials` | `list()` | 查询列表 |
| POST | `/api/materials` | `create()` | 创建业务记录 |
| DELETE | `/api/materials/{id}` | `delete()` | 删除业务记录 |
| GET | `/api/materials/{id}` | `get()` | 查询单条记录或详情 |
| PUT | `/api/materials/{id}` | `update()` | 更新业务记录 |

源码：`backend/src/main/java/com/example/messystem/warehouse/controller/MaterialResource.java`

#### PickingTaskResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/picking-tasks` | `list()` | 查询列表 |
| POST | `/api/picking-tasks` | `create()` | 创建业务记录 |
| GET | `/api/picking-tasks/{id}` | `get()` | 查询单条记录或详情 |
| POST | `/api/picking-tasks/{id}/complete` | `complete()` | 完成业务任务 |

源码：`backend/src/main/java/com/example/messystem/warehouse/controller/PickingTaskResource.java`

#### RobotDeliveryTaskResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/robot-delivery-tasks` | `list()` | 查询列表 |
| POST | `/api/robot-delivery-tasks` | `create()` | 创建业务记录 |
| GET | `/api/robot-delivery-tasks/{id}` | `get()` | 查询单条记录或详情 |
| POST | `/api/robot-delivery-tasks/{id}/arrive` | `arrive()` | 确认物流到达 |
| POST | `/api/robot-delivery-tasks/{id}/confirm-receipt` | `confirmReceipt()` | 确认物流任务已经收货 |

源码：`backend/src/main/java/com/example/messystem/warehouse/controller/RobotDeliveryTaskResource.java`

#### RobotResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/robots` | `list()` | 查询列表 |
| POST | `/api/robots` | `create()` | 创建业务记录 |
| DELETE | `/api/robots/{id}` | `delete()` | 删除业务记录 |
| GET | `/api/robots/{id}` | `get()` | 查询单条记录或详情 |
| PUT | `/api/robots/{id}` | `update()` | 更新业务记录 |

源码：`backend/src/main/java/com/example/messystem/warehouse/controller/RobotResource.java`

#### WarehouseResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/warehouses` | `listWarehouses()` | 查询列表 |
| POST | `/api/warehouses` | `createWarehouse()` | 创建业务记录 |
| DELETE | `/api/warehouses/{id}` | `deleteWarehouse()` | 删除业务记录 |
| GET | `/api/warehouses/{id}` | `getWarehouse()` | 查询单条记录或详情 |
| PUT | `/api/warehouses/{id}` | `updateWarehouse()` | 更新业务记录 |
| GET | `/api/warehouses/locations` | `listLocations()` | 查询列表 |
| POST | `/api/warehouses/locations` | `createLocation()` | 创建业务记录 |
| DELETE | `/api/warehouses/locations/{id}` | `deleteLocation()` | 删除业务记录 |
| GET | `/api/warehouses/locations/{id}` | `getLocation()` | 查询单条记录或详情 |
| PUT | `/api/warehouses/locations/{id}` | `updateLocation()` | 更新业务记录 |

源码：`backend/src/main/java/com/example/messystem/warehouse/controller/WarehouseResource.java`

### 用户、权限与系统维护

#### AccessResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/access/account-applications` | `accountApplications()` | 执行 accountApplications 对应的业务步骤 |
| GET | `/api/access/permission-applications` | `permissionApplications()` | 执行 permissionApplications 对应的业务步骤 |
| GET | `/api/access/permissions` | `permissions()` | 执行 permissions 对应的业务步骤 |
| GET | `/api/access/roles` | `roles()` | 执行 roles 对应的业务步骤 |
| GET | `/api/access/system-maintenance` | `systemMaintenance()` | 执行 systemMaintenance 对应的业务步骤 |
| GET | `/api/access/users/{userId}/data-scopes` | `userDataScopes()` | 执行 userDataScopes 对应的业务步骤 |
| PUT | `/api/access/users/{userId}/data-scopes` | `updateUserDataScopes()` | 更新业务记录 |
| GET | `/api/access/users/{userId}/roles` | `userRoles()` | 执行 userRoles 对应的业务步骤 |
| PUT | `/api/access/users/{userId}/roles` | `assignRoles()` | 分配执行人员或资源 |

源码：`backend/src/main/java/com/example/messystem/access/controller/AccessResource.java`

### 用户与制造主数据

#### MasterDataResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/process-routes` | `listProcessRoutes()` | 查询列表 |
| POST | `/api/process-routes` | `createProcessRoute()` | 创建业务记录 |
| DELETE | `/api/process-routes/{id}` | `deleteProcessRoute()` | 删除业务记录 |
| PUT | `/api/process-routes/{id}` | `updateProcessRoute()` | 更新业务记录 |
| GET | `/api/product-boms` | `listAllBom()` | 查询列表 |
| POST | `/api/product-boms` | `createBom()` | 创建业务记录 |
| DELETE | `/api/product-boms/{id}` | `deleteBom()` | 删除业务记录 |
| PUT | `/api/product-boms/{id}` | `updateBom()` | 更新业务记录 |
| GET | `/api/production-lines` | `listProductionLines()` | 查询列表 |
| POST | `/api/production-lines` | `createProductionLine()` | 创建业务记录 |
| DELETE | `/api/production-lines/{id}` | `disableProductionLine()` | 停用业务对象 |
| PUT | `/api/production-lines/{id}` | `updateProductionLine()` | 更新业务记录 |
| GET | `/api/products` | `listProducts()` | 查询列表 |
| POST | `/api/products` | `createProduct()` | 创建业务记录 |
| DELETE | `/api/products/{id}` | `disableProduct()` | 停用业务对象 |
| PUT | `/api/products/{id}` | `updateProduct()` | 更新业务记录 |
| GET | `/api/products/{id}/bom` | `listBom()` | 查询列表 |
| POST | `/api/products/{id}/bom` | `createBom()` | 创建业务记录 |
| GET | `/api/sync-logs` | `listSyncLogs()` | 查询列表 |

源码：`backend/src/main/java/com/example/messystem/master/controller/MasterDataResource.java`

#### UserResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/users` | `list()` | 查询列表 |
| POST | `/api/users` | `create()` | 创建业务记录 |
| PUT | `/api/users/{userId}/role` | `updateRole()` | 更新业务记录 |

源码：`backend/src/main/java/com/example/messystem/master/controller/UserResource.java`

### 质量检验、质量追溯与返工

#### QualityInspectionResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/quality-inspections` | `list()` | 查询列表 |
| POST | `/api/quality-inspections` | `create()` | 创建业务记录 |
| GET | `/api/quality-inspections/{id}` | `get()` | 查询单条记录或详情 |
| POST | `/api/quality-inspections/{id}/assign` | `assign()` | 分配执行人员或资源 |
| GET | `/api/quality-inspections/{id}/items` | `listItems()` | 查询列表 |
| POST | `/api/quality-inspections/{id}/items` | `addItem()` | 执行 addItem 对应的业务步骤 |
| POST | `/api/quality-inspections/{id}/judge` | `judge()` | 执行 judge 对应的业务步骤 |
| POST | `/api/quality-inspections/{id}/submit` | `submit()` | 提交业务事项 |
| GET | `/api/quality-inspections/create-options` | `createOptions()` | 创建业务记录 |
| GET | `/api/quality-inspections/inspectors` | `inspectors()` | 检查运行状态 |

源码：`backend/src/main/java/com/example/messystem/quality/controller/QualityInspectionResource.java`

#### QualityTraceResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/quality-traces` | `listAll()` | 查询列表 |
| GET | `/api/quality-traces/{id}` | `get()` | 查询单条记录或详情 |
| GET | `/api/quality-traces/by-inspection/{inspectionId}` | `listByInspection()` | 查询列表 |
| GET | `/api/quality-traces/by-work-order/{workOrderId}` | `listByWorkOrderAlias()` | 查询列表 |
| GET | `/api/quality-traces/work-orders/{workOrderId}` | `listByWorkOrder()` | 查询列表 |

源码：`backend/src/main/java/com/example/messystem/quality/controller/QualityTraceResource.java`

#### ReworkOrderResource

| 方法 | 路径 | Java 方法 | 用例 |
|---|---|---|---|
| GET | `/api/rework-orders` | `listByInspection()` | 查询列表 |
| POST | `/api/rework-orders` | `createReworkOrder()` | 创建业务记录 |
| GET | `/api/rework-orders/{id}` | `getReworkOrder()` | 查询单条记录或详情 |
| POST | `/api/rework-orders/{id}/dispatch` | `dispatch()` | 派发业务任务 |
| POST | `/api/rework-orders/{id}/finish` | `finish()` | 完成业务任务 |

源码：`backend/src/main/java/com/example/messystem/quality/controller/ReworkOrderResource.java`

## 七、常见追问定位

- “接口为什么安全？”：看 `AuthFilter`、`AuthorizationPolicy`、`AuthenticatedUser`、`DataScopeService`。
- “密码和 Token 怎么保存？”：看 `PasswordHasher`、`TokenHasher`、`AuthDao`；数据库只存哈希。
- “库存为什么不会扣成负数？”：看 `WarehouseService` 的业务前置校验和 `WarehouseDao` 的同连接事务/行锁。
- “工单状态怎么流转？”：看 `WorkOrderResource` -> `WorkOrderService` -> `WorkOrderDao` 及操作日志。
- “AI 会不会乱改生产数据？”：看 `AiPlanningClient`、`AiPlanningAdviceService` 和 `AiPlanningPanel`；只返回建议并回填人工表单。
- “二维码如何追溯？”：看 `TireLabelResource`、`TireTraceService`、`TraceFileService`、`PublicTireTraceResource`。
- “数据库/服务器在哪里配置？”：看 `.env.example`、`DbConfig`、`run-backend.sh`、Nginx 与 Supervisor 配置。
- “分层如何保证不被破坏？”：看 `ArchitectureLayerTest` 和每个 Java 文件头的答辩定位。

